export declare class Order {
    id: string;
    userId: string;
    product: string;
    quantity: number;
    price: number;
    constructor(id: string, userId: string, product: string, quantity: number, price: number);
}
